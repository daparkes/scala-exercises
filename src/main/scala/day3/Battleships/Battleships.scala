package day3.Battleships

import Array.ofDim

object Battleships {

  def main(args: Array[String]): Unit = {
    val board = new Board(3, 3)
    board.makeBoard()
    val ship = Ship(3, (0,0))


  }
}
